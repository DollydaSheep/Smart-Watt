"use client"; // only if it uses hooks (useState, useEffect, etc.)

export default function Navbar() {
  return (
    <nav className="p-4 bg-gray-800 text-white">
      <h1>My Navbar</h1>
    </nav>
  );
}